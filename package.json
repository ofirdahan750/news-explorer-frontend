{
  "name": "news-explorer-ofir",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.3.0",
    "@testing-library/user-event": "^13.5.0",
    "deploy": "^1.0.3",
    "npm": "^8.16.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.3.0",
    "react-scripts": "5.0.1",
    "run": "^1.4.0",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "BROWSER=none react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "clean":"rm -rf node_modules && npm i",
    "cleandev": "rm -rf node_modules && npm i && npm run start",
    "push": "git add . && git commit -m \"Hero cover height + page mobile first css\" && git push",
    "deploy": "rm -rf build && npm run build && scp -r ./build/* ofirdahan1003@news-explorer-ofir.students.nomoredomainssbs.ru:/home/ofirdahan1003/news-explorer-frontend",
    "fullpush": "npm run push && npm run deploy",
    "cleanfullpush": "npm run clean && npm run fullpush"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
